<h1>Sign In</h1>

<%= simple_form_for User.new, url: sessions_path do |f| %>
  
  <%= f.input :email %>
  <%= f.input :password %>
  
  <div class="actions">
    <br />
    <%= f.submit 'Sign In', class: 'btn btn-primary' %>
  </div>
  
<% end %>
